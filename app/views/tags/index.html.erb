<h3>Tags List</h3>

<ul>
<% @tags.each do |tag| %>
<li>
<%= link_to tag.title, tag_path(tag) %>:
<%= link_to "Edit", edit_tag_path(tag) %>
<%= link_to "Delete", tag_path(tag), method: "delete" %>
</li>
<% end %>
</ul>

<br><br>
<%= link_to "<< Back to Companies List <<", root_path %>
